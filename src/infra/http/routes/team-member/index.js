const { Router } = require('express');

const createTeamMemberRoute = () => {
  const router = new Router();

  // call the route files

  return router;
};

module.exports = { createTeamMemberRoute };
